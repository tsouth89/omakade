-- Schema generated by v1.6.0 constructors at c91b14e40437a14849f37c188d5762c12655299e
PRAGMA user_version=9;
CREATE TABLE achievement_summary (app_id TEXT PRIMARY KEY REFERENCES games(app_id), unlocked INTEGER NOT NULL, total INTEGER NOT NULL, source TEXT NOT NULL, updated_at INTEGER NOT NULL);
CREATE TABLE achievements (app_id TEXT NOT NULL REFERENCES games(app_id), api_name TEXT NOT NULL, title TEXT NOT NULL, description TEXT, icon_url TEXT, icon_path TEXT, unlocked INTEGER NOT NULL, unlock_time INTEGER NOT NULL, rarity REAL NOT NULL, hidden INTEGER NOT NULL, current_progress REAL NOT NULL, maximum_progress REAL NOT NULL, source TEXT NOT NULL, PRIMARY KEY(app_id, api_name));
CREATE TABLE artwork_overrides (source TEXT NOT NULL, runner TEXT NOT NULL, app_id TEXT NOT NULL, cover_path TEXT NOT NULL, PRIMARY KEY(source, runner, app_id));
CREATE TABLE battlenet_games (game_id TEXT PRIMARY KEY, product_id TEXT NOT NULL, name TEXT NOT NULL, launch_code TEXT, install_path TEXT, wine_prefix TEXT, runner TEXT, cover_path TEXT, hero_path TEXT, last_played INTEGER NOT NULL DEFAULT 0, flatpak INTEGER NOT NULL DEFAULT 0, favorite INTEGER NOT NULL DEFAULT 0, hidden INTEGER NOT NULL DEFAULT 0, observed_at INTEGER NOT NULL);
CREATE TABLE collection_games (collection_name TEXT NOT NULL, source TEXT NOT NULL, runner TEXT NOT NULL, app_id TEXT NOT NULL, PRIMARY KEY(collection_name, source, runner, app_id));
CREATE TABLE collections (name TEXT PRIMARY KEY COLLATE NOCASE, created_at INTEGER NOT NULL);
CREATE TABLE faugus_games (game_id TEXT PRIMARY KEY, name TEXT NOT NULL, executable_path TEXT, runner TEXT, cover_path TEXT, hero_path TEXT, playtime_seconds INTEGER NOT NULL DEFAULT 0, flatpak INTEGER NOT NULL DEFAULT 0, favorite INTEGER NOT NULL DEFAULT 0, hidden INTEGER NOT NULL DEFAULT 0, observed_at INTEGER NOT NULL);
CREATE TABLE game_insights (source TEXT NOT NULL, app_id TEXT NOT NULL, provider TEXT NOT NULL, provider_game_id INTEGER NOT NULL, title TEXT NOT NULL, critic_score INTEGER NOT NULL DEFAULT -1, critic_review_count INTEGER NOT NULL DEFAULT 0, rushed_seconds INTEGER NOT NULL DEFAULT 0, normal_seconds INTEGER NOT NULL DEFAULT 0, complete_seconds INTEGER NOT NULL DEFAULT 0, time_sample_count INTEGER NOT NULL DEFAULT 0, updated_at INTEGER NOT NULL, PRIMARY KEY(source, app_id, provider));
CREATE TABLE game_link_members (group_id TEXT NOT NULL, source TEXT NOT NULL, runner TEXT NOT NULL, app_id TEXT NOT NULL, is_primary INTEGER NOT NULL DEFAULT 0, PRIMARY KEY(source, runner, app_id));
CREATE TABLE game_organization (source TEXT NOT NULL, runner TEXT NOT NULL, app_id TEXT NOT NULL, completion_status TEXT NOT NULL DEFAULT '', tags_json TEXT NOT NULL DEFAULT '[]', PRIMARY KEY(source, runner, app_id));
CREATE TABLE games (app_id TEXT PRIMARY KEY, title TEXT NOT NULL, favorite INTEGER NOT NULL DEFAULT 0, hidden INTEGER NOT NULL DEFAULT 0);
CREATE TABLE heroic_games (game_key TEXT PRIMARY KEY, app_id TEXT NOT NULL, runner TEXT NOT NULL, name TEXT NOT NULL, directory TEXT, cover_path TEXT, hero_path TEXT, flatpak INTEGER NOT NULL DEFAULT 0, favorite INTEGER NOT NULL DEFAULT 0, hidden INTEGER NOT NULL DEFAULT 0, observed_at INTEGER NOT NULL, playtime_minutes INTEGER NOT NULL DEFAULT 0, last_played INTEGER NOT NULL DEFAULT 0);
CREATE TABLE installations (app_id TEXT PRIMARY KEY REFERENCES games(app_id), install_dir TEXT NOT NULL, library_path TEXT NOT NULL, manifest_path TEXT NOT NULL, cover_path TEXT, hero_path TEXT, logo_path TEXT, last_played INTEGER NOT NULL DEFAULT 0, playtime_minutes INTEGER NOT NULL DEFAULT 0, observed_at INTEGER NOT NULL);
CREATE TABLE launch_activity (source TEXT NOT NULL, runner TEXT NOT NULL, app_id TEXT NOT NULL, last_launched INTEGER NOT NULL, launch_count INTEGER NOT NULL DEFAULT 1, PRIMARY KEY(source, runner, app_id));
CREATE TABLE lutris_games (id TEXT PRIMARY KEY, slug TEXT NOT NULL, name TEXT NOT NULL, runner TEXT, directory TEXT, platform TEXT, year INTEGER NOT NULL DEFAULT 0, last_played INTEGER NOT NULL DEFAULT 0, playtime_minutes INTEGER NOT NULL DEFAULT 0, cover_path TEXT, flatpak INTEGER NOT NULL DEFAULT 0, favorite INTEGER NOT NULL DEFAULT 0, hidden INTEGER NOT NULL DEFAULT 0, observed_at INTEGER NOT NULL);
CREATE TABLE owned_games (steam_id TEXT NOT NULL, app_id TEXT NOT NULL REFERENCES games(app_id), playtime_minutes INTEGER NOT NULL DEFAULT 0, synced_at INTEGER NOT NULL, PRIMARY KEY(steam_id, app_id));
CREATE TABLE pcsx2_games (game_id TEXT PRIMARY KEY, name TEXT NOT NULL, path TEXT NOT NULL, serial TEXT, region TEXT, cover_path TEXT, last_played INTEGER NOT NULL DEFAULT 0, playtime_seconds INTEGER NOT NULL DEFAULT 0, is_elf INTEGER NOT NULL DEFAULT 0, flatpak INTEGER NOT NULL DEFAULT 0, favorite INTEGER NOT NULL DEFAULT 0, hidden INTEGER NOT NULL DEFAULT 0, observed_at INTEGER NOT NULL);
CREATE TABLE retroarch_games (game_id TEXT PRIMARY KEY, name TEXT NOT NULL, content_path TEXT NOT NULL, core_path TEXT, core_name TEXT, cover_path TEXT, hero_path TEXT, system TEXT NOT NULL DEFAULT '', playtime_seconds INTEGER NOT NULL DEFAULT 0, last_played INTEGER NOT NULL DEFAULT 0, flatpak INTEGER NOT NULL DEFAULT 0, favorite INTEGER NOT NULL DEFAULT 0, hidden INTEGER NOT NULL DEFAULT 0, observed_at INTEGER NOT NULL);
CREATE TABLE ryujinx_games (game_id TEXT PRIMARY KEY, name TEXT NOT NULL, path TEXT, title_id TEXT, last_played INTEGER NOT NULL DEFAULT 0, playtime_seconds INTEGER NOT NULL DEFAULT 0, cover_path TEXT, flatpak INTEGER NOT NULL DEFAULT 0, flatpak_app_id TEXT NOT NULL DEFAULT '', favorite INTEGER NOT NULL DEFAULT 0, hidden INTEGER NOT NULL DEFAULT 0, observed_at INTEGER NOT NULL);
CREATE TABLE source_state (source TEXT PRIMARY KEY, last_scan INTEGER, last_error TEXT, paths TEXT NOT NULL DEFAULT '');
